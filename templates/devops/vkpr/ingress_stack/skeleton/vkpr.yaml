global:
  domain: ${{ values.domain }} 
  ingressClassName: nginx
  secure: true
  provider: aws
cert-manager:
  email: ${{ values.email }} 
  issuer:
    type: "production"
    solver: "DNS01"
external-dns:
  enabled: true
ingress:
  enabled: true